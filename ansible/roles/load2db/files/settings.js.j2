const db_connection_prop = {
  host: "{{ db.host }}",
  user: "{{ db.user.name }}",
  password: "{{ db.user.password }}",
  database : '{{ db.name }}'
};

exports.db_connection_prop = db_connection_prop;
